<h1>Create a review for  <%= @restaurant.name %>  </h1>

<%= simple_form_for([@restaurant,@review]) do |r| %>
  <%= r.input :rating, collection: (0..5) %>
  <%= r.input :content %>
  <%= r.submit "submit your nice review :)" %>
<% end %>
